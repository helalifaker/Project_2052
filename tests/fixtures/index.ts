/**
 * Test Fixtures Index
 *
 * Re-exports all fixtures for convenient importing.
 */

export {
  test,
  expect,
  type Role,
  hasCredentials,
  hasValidCachedSession,
  authenticateAs,
  getStorageStatePath,
  setupAuthState,
} from "./auth";
